// @pignal/render — Shared rendering components, utilities, and static assets.
// Used by @pignal/templates (template JSX) and @pignal/web (admin pages).

export type {
  VisitorContext,
  TemplateVocabulary,
  TemplateSeoHints,
  I18nProps,
  OptionalI18nProps,
} from './types';
